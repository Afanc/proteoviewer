# ProteoViewer deployment env (example/template)
# Copy to e.g. /etc/proteoviewer/proteoviewer.env or /opt/ProteoViewer/proteoviewer.env
# Then point the systemd unit's EnvironmentFile=... to that path.

# Server bind settings
PV_ADDRESS=0.0.0.0
PV_PORT=5007

# Workers (Panel multi-process)
PV_NUM_PROCS=8

# Websocket origins (Bokeh/Panel). Comma-separated.
# Use your public hostname; add ":<port>" if you are not terminating/rewriting ports in a reverse proxy.
PV_ALLOW_ORIGINS=proteoviewer.example.org,proteoviewer.example.org:5007

# Upload directory for server-mode uploads
PV_UPLOAD_DIR=/srv/proteoviewer/uploads

# Upload your facility's logo
PV_FACILITY_LOGO_RESOURCE=Biozentrum_Logo_2011.png

# Disable native file dialogs in server mode (recommended for multi-user servers)
PV_ENABLE_SYSTEM_DIALOG=0
