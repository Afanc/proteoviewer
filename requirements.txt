# Core numerics / IO
numpy>=1.24,<2.2
pandas
tqdm
pyyaml
openpyxl

# AnnData stack (pin because you said there is a reason)
anndata==0.11.4
scanpy==1.11.0
numba==0.61.0
scipy
h5py

# Viz / UI
panel
bokeh
plotly
matplotlib

# ML / utilities used by the app
scikit-learn
scikit-misc

# Dataframes / backend helpers
polars

# CLI / prompts (if ProteoViewer actually uses these; keep if you do)
typer[all]
InquirerPy

# Imaging dependency sometimes pulled by Panel/Bokeh usage (safe to include)
pillow
