# ProteoViewer deployment env (Biozentrum/PCF - current)
# Keep this file on the server at: /opt/ProteoViewer/proteoviewer.env

# Server bind settings
PV_ADDRESS=0.0.0.0
PV_PORT=5007

# Workers
PV_NUM_PROCS=8

# Websocket origins (Bokeh/Panel). Include host:port (and optionally host without port).
PV_ALLOW_ORIGINS=131.152.17.97:5007,proteoviewer.biozentrum.unibas.ch,proteoviewer.biozentrum.unibas.ch:5007

# Upload directory for server-mode uploads
PV_UPLOAD_DIR=/mnt/DATA/proteoviewer_uploads

# Disable native file dialogs in server mode
PV_ENABLE_SYSTEM_DIALOG=0
